<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.stn.carterminal">

    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.WRITE_PROFILE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.CheckVehicle.CheckVehicleActivity"></activity>
        <activity
            android:name=".activity.ScanVehicleActivity"
            android:launchMode="singleTask" />
        <activity android:name=".activity.DetailVehicleActivity" />
        <activity android:name=".activity.SearchVehicleActivity" />
        <activity android:name=".activity.DetailManifestActivity" />
        <activity android:name=".activity.SearchManifestActivity" />
        <activity
            android:name=".activity.HomeActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".activity.SettingActivity" />
        <activity android:name=".activity.SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>